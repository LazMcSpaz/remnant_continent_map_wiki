{
  "name": "remnant-continent-atlas",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Interactive worldbuilding atlas for The Remnant Continent — Phase 1 map spine.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "maplibre-gl": "^4.7.1"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
